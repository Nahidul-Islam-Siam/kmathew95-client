import SupportPage from '@/app/(common)/messages/page';
import React from 'react';

const Messages = () => {
    return (
        <div>
            <SupportPage />
        </div>
    );
};

export default Messages;